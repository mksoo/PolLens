# PolLens Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Claude Code skill `/pollens` that scrapes 선관위 공약 data from policy.nec.go.kr via Playwright and presents a candidate comparison for 동탄5동 voters.

**Architecture:** Playwright TypeScript scraper extracts textLayer text from policy.nec.go.kr's document viewer, parses it into structured Candidate objects, and saves them as individual Markdown files per candidate. The `pollens.md` Claude Code skill reads these files on demand and presents a comparison table.

**Tech Stack:** TypeScript 5, Playwright 1.44, ts-node 10, vitest 1

---

## File Map

| File | Role |
|------|------|
| `package.json` | Node.js 프로젝트 설정 |
| `tsconfig.json` | TypeScript 설정 (CommonJS) |
| `vitest.config.ts` | 테스트 러너 설정 |
| `scripts/types.ts` | 공통 타입: Candidate, Pledge, ScraperConfig, CacheMeta, ElectionType |
| `scripts/parser.ts` | 순수 함수: 원시 textLayer 텍스트 → Candidate 구조체 |
| `scripts/mdWriter.ts` | 순수 함수: Candidate → Markdown 문자열 + 파일 저장 |
| `scripts/cache.ts` | 순수 함수: data/meta.json 확인/갱신 |
| `scripts/explore.ts` | 1회성: policy.nec.go.kr DOM 구조 탐색, 셀렉터 발견 |
| `scripts/scrapers/base.ts` | Playwright: 브라우저 관리, textLayer 추출, 페이지 순회 |
| `scripts/scrapers/governor.ts` | 도지사 후보 수집 |
| `scripts/scrapers/mayor.ts` | 시장 후보 수집 |
| `scripts/scrapers/provincial.ts` | 도의원 후보 수집 |
| `scripts/scrapers/municipal.ts` | 시의원 후보 수집 |
| `scripts/scrapers/education.ts` | 교육감 후보 수집 |
| `scripts/collect.ts` | CLI 진입점: 모든 스크래퍼 조율 |
| `tests/parser.test.ts` | parser.ts 유닛 테스트 |
| `tests/mdWriter.test.ts` | mdWriter.ts 유닛 테스트 |
| `tests/cache.test.ts` | cache.ts 유닛 테스트 |
| `skills/pollens.md` | Claude Code 스킬 정의 |

---

## Task 1: 프로젝트 설정

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `vitest.config.ts`
- Create: `.gitignore`

- [ ] **Step 1: package.json 생성**

```json
{
  "name": "pollens",
  "version": "1.0.0",
  "description": "2026 지방선거 공약 비교 Claude Code 스킬",
  "scripts": {
    "collect": "npx ts-node scripts/collect.ts",
    "explore": "npx ts-node scripts/explore.ts",
    "test": "npx vitest run",
    "test:watch": "npx vitest"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0",
    "vitest": "^1.6.0"
  }
}
```

- [ ] **Step 2: tsconfig.json 생성**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "moduleResolution": "node",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "."
  },
  "include": ["scripts/**/*", "tests/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

- [ ] **Step 3: vitest.config.ts 생성**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['tests/**/*.test.ts'],
  },
});
```

- [ ] **Step 4: .gitignore 생성**

```
node_modules/
dist/
data/
exploration/
*.png
```

- [ ] **Step 5: 의존성 설치**

```bash
cd /Users/mksoo/Documents/dev/tmp/PolLens
npm install
npx playwright install chromium
```

Expected: `node_modules/` 생성, chromium 브라우저 다운로드

- [ ] **Step 6: 디렉토리 구조 생성**

```bash
mkdir -p scripts/scrapers tests skills data exploration
```

- [ ] **Step 7: 커밋**

```bash
git init
git add package.json tsconfig.json vitest.config.ts .gitignore
git commit -m "chore: project setup — Node.js, TypeScript, Playwright, vitest"
```

---

## Task 2: 공통 타입 정의

**Files:**
- Create: `scripts/types.ts`

- [ ] **Step 1: types.ts 작성**

```typescript
// scripts/types.ts

export type ElectionType = '도지사' | '시장' | '도의원' | '시의원' | '교육감';

export interface Pledge {
  rank: number;
  title: string;
  goal: string[];
  method: string[];
  period: string[];
  budget: string[];
}

export interface Candidate {
  name: string;
  ballotNumber: number;
  party: string;
  electionType: ElectionType;
  /** 지역 파일명 디렉토리 (예: 경기도, 화성시) */
  region: string;
  /** 실제 선거구명 (예: 동탄제1선거구, 동탄5동) */
  district: string;
  pledges: Pledge[];
  collectedAt: string; // ISO8601
}

export interface ScraperConfig {
  electionType: ElectionType;
  /** data/<electionType>/<region>/ 경로에 사용 */
  region: string;
  /** 선관위 사이트에서 필터링할 선거구명 */
  district: string;
}

export interface CandidateRef {
  documentKey: string;
  name: string;
  ballotNumber: number;
  party: string;
}

export interface CacheMeta {
  collectedAt: string; // ISO8601
  elections: ElectionType[];
}
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add scripts/types.ts
git commit -m "feat: add shared TypeScript types"
```

---

## Task 3: 텍스트 파서 (TDD)

**Files:**
- Create: `scripts/parser.ts`
- Create: `tests/parser.test.ts`

실제 확인된 텍스트 포맷 기반으로 작성.

- [ ] **Step 1: 테스트 파일 작성**

```typescript
// tests/parser.test.ts
import { describe, it, expect } from 'vitest';
import { parseRawText, parsePledges, extractSection } from '../scripts/parser';

// 실제 policy.nec.go.kr에서 복사한 원문
const SAMPLE_RAW_TEXT = `선거명 경기도지사선거 선거구명 경기도후보자명 추미애 기호 1 소속정당명 더불어민주당공약순위: 1 제목 : 수도권 30분 출근 대전환□ 목 표○ 길 위에 버리는 시간을 도민에게 돌려주기 위한 광역교통망 구축○ 이동에 대한 부담과 피로도를 덜어 도민의 보편적 이동권 보장을 확대□ 이행방법○ GTX 지체 없는 개통 추진○ 수도권 원(One)패스 추진□ 이행기간○ 2026년~2030년 추진□ 재원조달방안 등○ 2026년의 경우 기존 예산의 조정 또는 추가경정예산 편성을 통해 추진공약순위: 2 제목 : 경기도 청년 기회 확대□ 목 표○ 청년 일자리·주거·문화 지원 강화□ 이행방법○ 청년 특별 채용 확대○ 청년 임대주택 공급□ 이행기간○ 2026년~2027년□ 재원조달방안 등○ 도 자체 예산으로 추진`;

describe('extractSection', () => {
  it('목 표 섹션에서 ○ 항목을 추출한다', () => {
    const result = extractSection(SAMPLE_RAW_TEXT, '목\\s*표');
    expect(result).toContain('길 위에 버리는 시간을 도민에게 돌려주기 위한 광역교통망 구축');
    expect(result).toContain('이동에 대한 부담과 피로도를 덜어 도민의 보편적 이동권 보장을 확대');
  });

  it('이행방법 섹션에서 ○ 항목을 추출한다', () => {
    const result = extractSection(SAMPLE_RAW_TEXT, '이행방법');
    expect(result).toContain('GTX 지체 없는 개통 추진');
  });

  it('해당 섹션이 없으면 빈 배열을 반환한다', () => {
    const result = extractSection('아무 텍스트', '목\\s*표');
    expect(result).toEqual([]);
  });
});

describe('parsePledges', () => {
  it('공약순위 마커로 공약 블록을 분리한다', () => {
    const pledges = parsePledges(SAMPLE_RAW_TEXT);
    expect(pledges).toHaveLength(2);
    expect(pledges[0].rank).toBe(1);
    expect(pledges[0].title).toBe('수도권 30분 출근 대전환');
    expect(pledges[1].rank).toBe(2);
    expect(pledges[1].title).toBe('경기도 청년 기회 확대');
  });

  it('각 공약의 목표 항목을 파싱한다', () => {
    const pledges = parsePledges(SAMPLE_RAW_TEXT);
    expect(pledges[0].goal).toContain('길 위에 버리는 시간을 도민에게 돌려주기 위한 광역교통망 구축');
  });

  it('각 공약의 이행방법 항목을 파싱한다', () => {
    const pledges = parsePledges(SAMPLE_RAW_TEXT);
    expect(pledges[0].method).toContain('GTX 지체 없는 개통 추진');
  });

  it('각 공약의 이행기간 항목을 파싱한다', () => {
    const pledges = parsePledges(SAMPLE_RAW_TEXT);
    expect(pledges[0].period).toContain('2026년~2030년 추진');
  });

  it('각 공약의 재원조달 항목을 파싱한다', () => {
    const pledges = parsePledges(SAMPLE_RAW_TEXT);
    expect(pledges[0].budget).toContain('2026년의 경우 기존 예산의 조정 또는 추가경정예산 편성을 통해 추진');
  });
});

describe('parseRawText', () => {
  it('헤더에서 후보자 정보를 추출한다', () => {
    const candidate = parseRawText(SAMPLE_RAW_TEXT, '도지사', '경기도');
    expect(candidate.name).toBe('추미애');
    expect(candidate.ballotNumber).toBe(1);
    expect(candidate.party).toBe('더불어민주당');
    expect(candidate.district).toBe('경기도');
    expect(candidate.electionType).toBe('도지사');
  });

  it('공약 목록을 포함한다', () => {
    const candidate = parseRawText(SAMPLE_RAW_TEXT, '도지사', '경기도');
    expect(candidate.pledges).toHaveLength(2);
  });

  it('collectedAt이 ISO8601 형식이다', () => {
    const candidate = parseRawText(SAMPLE_RAW_TEXT, '도지사', '경기도');
    expect(() => new Date(candidate.collectedAt)).not.toThrow();
    expect(new Date(candidate.collectedAt).toISOString()).toBe(candidate.collectedAt);
  });

  it('파싱 불가능한 텍스트에서 오류를 던진다', () => {
    expect(() => parseRawText('이상한 텍스트', '도지사', '경기도')).toThrow();
  });
});
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

```bash
npx vitest run tests/parser.test.ts
```

Expected: `FAIL` — `Cannot find module '../scripts/parser'`

- [ ] **Step 3: parser.ts 구현**

```typescript
// scripts/parser.ts
import type { Candidate, Pledge, ElectionType } from './types';

export function extractSection(text: string, sectionName: string): string[] {
  const sectionPattern = new RegExp(`□\\s*${sectionName}(.+?)(?=□|공약순위:|$)`, 's');
  const match = text.match(sectionPattern);
  if (!match) return [];

  const itemPattern = /○\s*(.+?)(?=○|$)/gs;
  const items: string[] = [];
  let itemMatch: RegExpExecArray | null;

  while ((itemMatch = itemPattern.exec(match[1])) !== null) {
    const item = itemMatch[1].trim();
    if (item) items.push(item);
  }
  return items;
}

export function parsePledges(text: string): Pledge[] {
  // 공약순위: N 제목 : [제목] 패턴으로 분리
  const pledgePattern = /공약순위:\s*(\d+)\s*제목\s*:\s*(.+?)(?=공약순위:|$)/gs;
  const pledges: Pledge[] = [];
  let match: RegExpExecArray | null;

  while ((match = pledgePattern.exec(text)) !== null) {
    const rank = parseInt(match[1], 10);
    const block = match[0];
    // 제목은 □ 앞까지
    const titleRaw = match[2];
    const title = titleRaw.split('□')[0].trim();

    pledges.push({
      rank,
      title,
      goal: extractSection(block, '목\\s*표'),
      method: extractSection(block, '이행방법'),
      period: extractSection(block, '이행기간'),
      budget: extractSection(block, '재원조달방안'),
    });
  }

  return pledges;
}

export function parseRawText(
  rawText: string,
  electionType: ElectionType,
  region: string
): Candidate {
  const nameMatch = rawText.match(/후보자명\s+(\S+)/);
  const ballotMatch = rawText.match(/기호\s+(\d+)/);
  const partyMatch = rawText.match(/소속정당명\s+(.+?)(?=공약순위|$)/s);
  const districtMatch = rawText.match(/선거구명\s+(\S+)/);

  if (!nameMatch || !ballotMatch || !partyMatch) {
    throw new Error(
      `후보자 헤더 파싱 실패. 텍스트 앞 100자: "${rawText.slice(0, 100)}"`
    );
  }

  return {
    name: nameMatch[1].trim(),
    ballotNumber: parseInt(ballotMatch[1], 10),
    party: partyMatch[1].trim(),
    electionType,
    region,
    district: districtMatch?.[1].trim() ?? region,
    pledges: parsePledges(rawText),
    collectedAt: new Date().toISOString(),
  };
}
```

- [ ] **Step 4: 테스트 재실행 — PASS 확인**

```bash
npx vitest run tests/parser.test.ts
```

Expected: 모든 테스트 PASS

- [ ] **Step 5: 커밋**

```bash
git add scripts/parser.ts tests/parser.test.ts
git commit -m "feat: text parser with TDD — parseRawText, parsePledges, extractSection"
```

---

## Task 4: MD 라이터 (TDD)

**Files:**
- Create: `scripts/mdWriter.ts`
- Create: `tests/mdWriter.test.ts`

- [ ] **Step 1: 테스트 파일 작성**

```typescript
// tests/mdWriter.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import * as fs from 'fs';
import * as path from 'path';
import * as os from 'os';
import { candidateToMd, saveCandidateMd } from '../scripts/mdWriter';
import type { Candidate } from '../scripts/types';

const MOCK_CANDIDATE: Candidate = {
  name: '추미애',
  ballotNumber: 1,
  party: '더불어민주당',
  electionType: '도지사',
  region: '경기도',
  district: '경기도',
  collectedAt: '2026-05-28T10:00:00.000Z',
  pledges: [
    {
      rank: 1,
      title: '수도권 30분 출근 대전환',
      goal: ['광역교통망 구축'],
      method: ['GTX 지체 없는 개통 추진'],
      period: ['2026년~2030년 추진'],
      budget: ['추가경정예산 편성을 통해 추진'],
    },
  ],
};

describe('candidateToMd', () => {
  it('제목 줄에 이름, 선거유형, 지역을 포함한다', () => {
    const md = candidateToMd(MOCK_CANDIDATE);
    expect(md).toContain('# 추미애 — 도지사 · 경기도');
  });

  it('기호번호, 정당, 수집일을 메타 줄에 포함한다', () => {
    const md = candidateToMd(MOCK_CANDIDATE);
    expect(md).toContain('기호: 1');
    expect(md).toContain('더불어민주당');
    expect(md).toContain('2026-05-28T10:00:00.000Z');
  });

  it('공약 제목을 포함한다', () => {
    const md = candidateToMd(MOCK_CANDIDATE);
    expect(md).toContain('### 공약 1: 수도권 30분 출근 대전환');
  });

  it('목표 항목을 - 리스트로 변환한다', () => {
    const md = candidateToMd(MOCK_CANDIDATE);
    expect(md).toContain('- 광역교통망 구축');
  });

  it('항목이 없으면 (정보 없음)을 표시한다', () => {
    const candidate: Candidate = {
      ...MOCK_CANDIDATE,
      pledges: [{ rank: 1, title: '테스트', goal: [], method: [], period: [], budget: [] }],
    };
    const md = candidateToMd(candidate);
    expect(md).toContain('- (정보 없음)');
  });
});

describe('saveCandidateMd', () => {
  let tmpDir: string;

  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'pollens-test-'));
  });

  afterEach(() => {
    fs.rmSync(tmpDir, { recursive: true });
  });

  it('올바른 경로에 파일을 생성한다', () => {
    saveCandidateMd(MOCK_CANDIDATE, tmpDir);
    const expectedPath = path.join(tmpDir, '도지사', '경기도', '추미애.md');
    expect(fs.existsSync(expectedPath)).toBe(true);
  });

  it('저장된 파일에 후보자 이름이 포함된다', () => {
    saveCandidateMd(MOCK_CANDIDATE, tmpDir);
    const content = fs.readFileSync(
      path.join(tmpDir, '도지사', '경기도', '추미애.md'),
      'utf-8'
    );
    expect(content).toContain('# 추미애');
  });
});
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

```bash
npx vitest run tests/mdWriter.test.ts
```

Expected: `FAIL` — `Cannot find module '../scripts/mdWriter'`

- [ ] **Step 3: mdWriter.ts 구현**

```typescript
// scripts/mdWriter.ts
import * as fs from 'fs';
import * as path from 'path';
import type { Candidate } from './types';

export function candidateToMd(candidate: Candidate): string {
  const header = `# ${candidate.name} — ${candidate.electionType} · ${candidate.district}
> 기호: ${candidate.ballotNumber} · ${candidate.party} · 수집: ${candidate.collectedAt}

---
`;

  const pledgeSections = candidate.pledges
    .map((p) => {
      const lines = [
        `### 공약 ${p.rank}: ${p.title}`,
        '',
        '**목표**',
        ...(p.goal.length > 0 ? p.goal.map((g) => `- ${g}`) : ['- (정보 없음)']),
        '',
        '**이행방법**',
        ...(p.method.length > 0 ? p.method.map((m) => `- ${m}`) : ['- (정보 없음)']),
        '',
        '**이행기간**',
        ...(p.period.length > 0 ? p.period.map((t) => `- ${t}`) : ['- (정보 없음)']),
        '',
        '**재원조달**',
        ...(p.budget.length > 0 ? p.budget.map((b) => `- ${b}`) : ['- (정보 없음)']),
      ];
      return lines.join('\n');
    })
    .join('\n\n---\n\n');

  return header + '\n' + pledgeSections + '\n';
}

export function saveCandidateMd(candidate: Candidate, dataDir: string): void {
  const dir = path.join(dataDir, candidate.electionType, candidate.region);
  fs.mkdirSync(dir, { recursive: true });
  const filePath = path.join(dir, `${candidate.name}.md`);
  fs.writeFileSync(filePath, candidateToMd(candidate), 'utf-8');
}
```

- [ ] **Step 4: 테스트 재실행 — PASS 확인**

```bash
npx vitest run tests/mdWriter.test.ts
```

Expected: 모든 테스트 PASS

- [ ] **Step 5: 커밋**

```bash
git add scripts/mdWriter.ts tests/mdWriter.test.ts
git commit -m "feat: markdown writer with TDD — candidateToMd, saveCandidateMd"
```

---

## Task 5: 캐시 매니저 (TDD)

**Files:**
- Create: `scripts/cache.ts`
- Create: `tests/cache.test.ts`

- [ ] **Step 1: 테스트 파일 작성**

```typescript
// tests/cache.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import * as fs from 'fs';
import * as path from 'path';
import * as os from 'os';
import { isCacheValid, updateMeta, readMeta } from '../scripts/cache';

describe('isCacheValid', () => {
  let tmpDir: string;

  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'pollens-cache-'));
  });

  afterEach(() => {
    fs.rmSync(tmpDir, { recursive: true });
  });

  it('meta.json이 없으면 false를 반환한다', () => {
    expect(isCacheValid(tmpDir)).toBe(false);
  });

  it('collectedAt이 24시간 이내면 true를 반환한다', () => {
    const meta = {
      collectedAt: new Date().toISOString(),
      elections: ['도지사'],
    };
    fs.writeFileSync(path.join(tmpDir, 'meta.json'), JSON.stringify(meta));
    expect(isCacheValid(tmpDir)).toBe(true);
  });

  it('collectedAt이 25시간 전이면 false를 반환한다', () => {
    const old = new Date(Date.now() - 25 * 60 * 60 * 1000);
    const meta = { collectedAt: old.toISOString(), elections: ['도지사'] };
    fs.writeFileSync(path.join(tmpDir, 'meta.json'), JSON.stringify(meta));
    expect(isCacheValid(tmpDir)).toBe(false);
  });
});

describe('updateMeta', () => {
  let tmpDir: string;

  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'pollens-meta-'));
  });

  afterEach(() => {
    fs.rmSync(tmpDir, { recursive: true });
  });

  it('meta.json을 생성한다', () => {
    updateMeta(tmpDir, ['도지사', '시장']);
    expect(fs.existsSync(path.join(tmpDir, 'meta.json'))).toBe(true);
  });

  it('저장된 elections 목록이 일치한다', () => {
    updateMeta(tmpDir, ['도지사', '시장']);
    const meta = readMeta(tmpDir);
    expect(meta?.elections).toEqual(['도지사', '시장']);
  });

  it('collectedAt이 현재 시각 근처이다', () => {
    const before = Date.now();
    updateMeta(tmpDir, ['시장']);
    const after = Date.now();
    const meta = readMeta(tmpDir);
    const collectedAt = new Date(meta!.collectedAt).getTime();
    expect(collectedAt).toBeGreaterThanOrEqual(before);
    expect(collectedAt).toBeLessThanOrEqual(after);
  });
});

describe('readMeta', () => {
  let tmpDir: string;

  beforeEach(() => {
    tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'pollens-read-'));
  });

  afterEach(() => {
    fs.rmSync(tmpDir, { recursive: true });
  });

  it('meta.json이 없으면 null을 반환한다', () => {
    expect(readMeta(tmpDir)).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

```bash
npx vitest run tests/cache.test.ts
```

Expected: `FAIL` — `Cannot find module '../scripts/cache'`

- [ ] **Step 3: cache.ts 구현**

```typescript
// scripts/cache.ts
import * as fs from 'fs';
import * as path from 'path';
import type { CacheMeta, ElectionType } from './types';

const CACHE_TTL_MS = 24 * 60 * 60 * 1000; // 24시간

export function getMetaPath(dataDir: string): string {
  return path.join(dataDir, 'meta.json');
}

export function isCacheValid(dataDir: string): boolean {
  const metaPath = getMetaPath(dataDir);
  if (!fs.existsSync(metaPath)) return false;

  const meta: CacheMeta = JSON.parse(fs.readFileSync(metaPath, 'utf-8'));
  const collectedAt = new Date(meta.collectedAt).getTime();
  return Date.now() - collectedAt < CACHE_TTL_MS;
}

export function updateMeta(dataDir: string, elections: ElectionType[]): void {
  fs.mkdirSync(dataDir, { recursive: true });
  const meta: CacheMeta = {
    collectedAt: new Date().toISOString(),
    elections,
  };
  fs.writeFileSync(getMetaPath(dataDir), JSON.stringify(meta, null, 2), 'utf-8');
}

export function readMeta(dataDir: string): CacheMeta | null {
  const metaPath = getMetaPath(dataDir);
  if (!fs.existsSync(metaPath)) return null;
  return JSON.parse(fs.readFileSync(metaPath, 'utf-8')) as CacheMeta;
}
```

- [ ] **Step 4: 테스트 재실행 — PASS 확인**

```bash
npx vitest run tests/cache.test.ts
```

Expected: 모든 테스트 PASS

- [ ] **Step 5: 전체 테스트 실행**

```bash
npx vitest run
```

Expected: parser, mdWriter, cache 모두 PASS

- [ ] **Step 6: 커밋**

```bash
git add scripts/cache.ts tests/cache.test.ts
git commit -m "feat: cache manager with TDD — isCacheValid, updateMeta, readMeta"
```

---

## Task 6: 사이트 탐색 (policy.nec.go.kr 셀렉터 발견)

**Files:**
- Create: `scripts/explore.ts`
- Create: `exploration/SCRAPER_NOTES.md` (탐색 결과 저장)

이 태스크는 대화형(headful) Playwright로 실제 사이트를 탐색하며 셀렉터를 발견하는 작업이다.

- [ ] **Step 1: 탐색 스크립트 작성**

```typescript
// scripts/explore.ts
import { chromium } from 'playwright';
import * as fs from 'fs';
import * as path from 'path';
import * as readline from 'readline';

const OUT_DIR = path.join(process.cwd(), 'exploration');
fs.mkdirSync(OUT_DIR, { recursive: true });

async function pause(msg: string): Promise<void> {
  const rl = readline.createInterface({ input: process.stdin, output: process.stdout });
  return new Promise((resolve) => rl.question(`\n[PAUSE] ${msg}\nEnter를 누르면 계속: `, () => {
    rl.close();
    resolve();
  }));
}

async function explore() {
  console.log('브라우저 시작 (headful 모드)...');
  const browser = await chromium.launch({ headless: false, slowMo: 200 });
  const page = await browser.newPage({ viewport: { width: 1400, height: 900 } });

  // 1. 홈페이지
  await page.goto('https://policy.nec.go.kr');
  await page.waitForLoadState('networkidle');
  await page.screenshot({ path: path.join(OUT_DIR, '01-home.png') });

  const navLinks = await page.$$eval('a', (links) =>
    links
      .map((a) => ({ text: a.textContent?.trim(), href: (a as HTMLAnchorElement).href }))
      .filter((l) => l.text && l.href.includes('policy.nec.go.kr'))
  );
  console.log('\n=== 네비게이션 링크 ===');
  console.log(JSON.stringify(navLinks.slice(0, 20), null, 2));

  await pause('홈 탐색 완료. 후보자 목록이 있는 페이지로 수동 이동하세요.');

  // 2. 후보자 목록 페이지
  await page.screenshot({ path: path.join(OUT_DIR, '02-candidate-list.png') });

  const candidateInfo = await page.evaluate(() => {
    const allElements = document.querySelectorAll('*');
    const results: string[] = [];
    allElements.forEach((el) => {
      const text = el.textContent?.trim() ?? '';
      if (text.includes('기호') && text.includes('후보') && el.children.length < 5) {
        results.push(`${el.tagName}.${el.className}: ${text.slice(0, 80)}`);
      }
    });
    return results.slice(0, 20);
  });
  console.log('\n=== 후보자 관련 DOM 요소 ===');
  candidateInfo.forEach((info) => console.log(info));

  await pause('후보자 1명 클릭해서 공보 문서를 여세요.');

  // 3. 문서 뷰어
  await page.screenshot({ path: path.join(OUT_DIR, '03-viewer.png') });

  const viewerInfo = await page.evaluate(() => {
    // textLayer 찾기
    const allDivs = document.querySelectorAll('div, span');
    const textLayerCandidates: string[] = [];
    allDivs.forEach((el) => {
      if (
        el.className &&
        typeof el.className === 'string' &&
        (el.className.toLowerCase().includes('text') ||
          el.className.toLowerCase().includes('layer'))
      ) {
        textLayerCandidates.push(
          `${el.tagName}.${el.className} — 텍스트: "${el.textContent?.slice(0, 60)}"`
        );
      }
    });
    return textLayerCandidates.slice(0, 30);
  });
  console.log('\n=== 뷰어 textLayer 후보 요소 ===');
  viewerInfo.forEach((info) => console.log(info));

  const currentUrl = page.url();
  console.log('\n=== 현재 URL ===');
  console.log(currentUrl);

  await pause('탐색 완료. 브라우저를 닫습니다.');
  await browser.close();

  // 결과 저장
  const notes = `# Scraper Notes
> 탐색일: ${new Date().toISOString()}

## 현재 URL (뷰어)
${currentUrl}

## 확인된 정보
<!-- 아래를 채워주세요 -->

### 후보자 목록 페이지
- URL 패턴: 
- 후보자 항목 셀렉터: 
- 이름 셀렉터: 
- 기호번호 셀렉터: 
- 정당 셀렉터: 
- 문서 링크 셀렉터: 

### 문서 뷰어
- textLayer 셀렉터: 
- 다음 페이지 버튼 셀렉터: 
- 전체 페이지 수 셀렉터: 
- 문서 key URL 패턴: 
`;
  fs.writeFileSync(path.join(OUT_DIR, 'SCRAPER_NOTES.md'), notes, 'utf-8');
  console.log('\n탐색 결과가 exploration/SCRAPER_NOTES.md에 저장됐습니다.');
  console.log('스크린샷: exploration/*.png');
}

explore().catch(console.error);
```

- [ ] **Step 2: 탐색 실행**

```bash
npx ts-node scripts/explore.ts
```

브라우저가 열리면:
1. `https://policy.nec.go.kr` 홈에서 화성시 관련 선거를 찾아 이동
2. 후보자 목록 페이지로 이동 → Enter
3. 후보자 1명 클릭하여 공보 문서 열기 → Enter
4. 탐색 종료 → Enter

- [ ] **Step 3: SCRAPER_NOTES.md 작성**

`exploration/SCRAPER_NOTES.md`를 열어 콘솔 출력을 보며 빈칸을 채운다:

```markdown
# Scraper Notes

### 후보자 목록 페이지
- URL 패턴: https://policy.nec.go.kr/...
- 후보자 항목 셀렉터: .XXX (실제 값으로 교체)
- 이름 셀렉터: .YYY
- 기호번호 셀렉터: .ZZZ
- 문서 링크 셀렉터: a[href*="..."]

### 문서 뷰어
- textLayer 셀렉터: .AAA (실제 값으로 교체)
- 다음 페이지 버튼 셀렉터: .BBB
- 전체 페이지 수 셀렉터: .CCC
```

- [ ] **Step 4: 커밋**

```bash
git add scripts/explore.ts exploration/SCRAPER_NOTES.md exploration/*.png
git commit -m "chore: site exploration script and discovered selectors"
```

---

## Task 7: 베이스 스크래퍼

**Files:**
- Create: `scripts/scrapers/base.ts`

exploration/SCRAPER_NOTES.md의 셀렉터를 사용한다. **아래 SELECTORS의 값을 SCRAPER_NOTES.md에서 확인한 실제 값으로 교체하고 시작한다.**

- [ ] **Step 1: base.ts 작성**

```typescript
// scripts/scrapers/base.ts
import { chromium, type Browser, type Page } from 'playwright';
import type { CandidateRef } from '../types';

// ⚠️ exploration/SCRAPER_NOTES.md에서 확인한 실제 셀렉터로 교체
export const SELECTORS = {
  // 후보자 목록 페이지
  candidateItem: '.candidate-item',         // 교체 필요
  candidateName: '.candidate-name',          // 교체 필요
  ballotNumber: '.ballot-number',            // 교체 필요
  party: '.party-name',                      // 교체 필요
  documentLink: 'a[href*="viewer"]',         // 교체 필요

  // 문서 뷰어
  textLayer: '[class*="textLayer"]',         // 교체 필요
  nextPageBtn: '[class*="nextPage"]',        // 교체 필요
  pageInfo: '[class*="pageInfo"]',           // 교체 필요 (전체 페이지 수 표시)
};

export async function launchBrowser(): Promise<Browser> {
  return chromium.launch({ headless: true });
}

/**
 * 현재 페이지의 textLayer에서 텍스트를 추출한다.
 * 페이지 로딩 완료를 기다린 후 추출.
 */
export async function extractCurrentPageText(page: Page): Promise<string> {
  await page.waitForSelector(SELECTORS.textLayer, { timeout: 30_000 });
  return page.evaluate((selector) => {
    const layers = document.querySelectorAll(selector);
    return Array.from(layers)
      .map((el) => el.textContent ?? '')
      .join('\n');
  }, SELECTORS.textLayer);
}

/**
 * 문서 뷰어의 모든 페이지를 순회하며 텍스트를 추출한다.
 * documentKey: SCRAPER_NOTES.md의 문서 key URL 패턴 참조
 */
export async function extractAllPagesText(
  page: Page,
  documentKey: string
): Promise<string> {
  const viewerUrl = `https://policy.nec.go.kr/viewer?key=${documentKey}`; // 교체 필요
  await page.goto(viewerUrl, { waitUntil: 'networkidle' });

  const allText: string[] = [];
  let pageNum = 0;

  while (true) {
    const pageText = await extractCurrentPageText(page);
    allText.push(pageText);
    pageNum++;

    // 다음 페이지 버튼 확인
    const nextBtn = page.locator(SELECTORS.nextPageBtn);
    const isDisabled = await nextBtn.isDisabled().catch(() => true);
    if (isDisabled) break;

    await nextBtn.click();
    // 새 페이지 로딩 대기 (textLayer가 업데이트되길 기다림)
    await page.waitForTimeout(600);
  }

  console.log(`  → ${pageNum}페이지 추출 완료`);
  return allText.join('\n');
}

/**
 * 후보자 목록 페이지에서 CandidateRef 목록을 수집한다.
 * listingUrl: SCRAPER_NOTES.md의 후보자 목록 URL 패턴 참조
 */
export async function extractCandidateRefs(
  page: Page,
  listingUrl: string
): Promise<CandidateRef[]> {
  await page.goto(listingUrl, { waitUntil: 'networkidle' });
  await page.waitForSelector(SELECTORS.candidateItem, { timeout: 20_000 });

  return page.$$eval(
    SELECTORS.candidateItem,
    (items, selectors) =>
      items.map((item) => {
        const linkEl = item.querySelector(selectors.documentLink) as HTMLAnchorElement | null;
        const href = linkEl?.href ?? '';
        const keyMatch = href.match(/key=([a-f0-9]+)/i);
        return {
          documentKey: keyMatch?.[1] ?? '',
          name: item.querySelector(selectors.candidateName)?.textContent?.trim() ?? '',
          ballotNumber: parseInt(
            item.querySelector(selectors.ballotNumber)?.textContent?.trim() ?? '0',
            10
          ),
          party: item.querySelector(selectors.party)?.textContent?.trim() ?? '',
        };
      }),
    SELECTORS
  );
}
```

- [ ] **Step 2: 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add scripts/scrapers/base.ts
git commit -m "feat: base scraper — browser launch, textLayer extraction, candidate listing"
```

---

## Task 8: 선거별 스크래퍼 5종

**Files:**
- Create: `scripts/scrapers/governor.ts`
- Create: `scripts/scrapers/mayor.ts`
- Create: `scripts/scrapers/provincial.ts`
- Create: `scripts/scrapers/municipal.ts`
- Create: `scripts/scrapers/education.ts`

각 스크래퍼는 동일한 패턴을 따른다. **listingUrl은 exploration/SCRAPER_NOTES.md에서 확인한 실제 URL로 교체한다.**

- [ ] **Step 1: governor.ts 작성**

```typescript
// scripts/scrapers/governor.ts
import { chromium } from 'playwright';
import type { ScraperConfig } from '../types';
import { parseRawText } from '../parser';
import { saveCandidateMd } from '../mdWriter';
import { extractCandidateRefs, extractAllPagesText } from './base';

/**
 * 도지사 후보 공약을 수집한다.
 * listingUrl: 경기도지사 후보자 목록 URL (SCRAPER_NOTES.md 참조)
 */
export async function scrapeGovernor(
  config: ScraperConfig,
  dataDir: string
): Promise<void> {
  // ⚠️ SCRAPER_NOTES.md의 후보자 목록 URL 패턴으로 교체
  const listingUrl = `https://policy.nec.go.kr/...?election=도지사&district=${encodeURIComponent(config.district)}`;

  const browser = await chromium.launch({ headless: true });
  try {
    const context = await browser.newContext();
    const page = await context.newPage();

    const refs = await extractCandidateRefs(page, listingUrl);
    console.log(`  도지사 후보 ${refs.length}명 발견`);

    for (const ref of refs) {
      console.log(`  수집 중: 기호${ref.ballotNumber} ${ref.name}`);
      const rawText = await extractAllPagesText(page, ref.documentKey);
      const candidate = parseRawText(rawText, config.electionType, config.region);
      saveCandidateMd(candidate, dataDir);
    }
  } finally {
    await browser.close();
  }
}
```

- [ ] **Step 2: mayor.ts 작성**

```typescript
// scripts/scrapers/mayor.ts
import { chromium } from 'playwright';
import type { ScraperConfig } from '../types';
import { parseRawText } from '../parser';
import { saveCandidateMd } from '../mdWriter';
import { extractCandidateRefs, extractAllPagesText } from './base';

export async function scrapeMayor(
  config: ScraperConfig,
  dataDir: string
): Promise<void> {
  // ⚠️ SCRAPER_NOTES.md의 시장 목록 URL로 교체
  const listingUrl = `https://policy.nec.go.kr/...?election=시장&district=${encodeURIComponent(config.district)}`;

  const browser = await chromium.launch({ headless: true });
  try {
    const context = await browser.newContext();
    const page = await context.newPage();
    const refs = await extractCandidateRefs(page, listingUrl);
    console.log(`  시장 후보 ${refs.length}명 발견`);
    for (const ref of refs) {
      console.log(`  수집 중: 기호${ref.ballotNumber} ${ref.name}`);
      const rawText = await extractAllPagesText(page, ref.documentKey);
      const candidate = parseRawText(rawText, config.electionType, config.region);
      saveCandidateMd(candidate, dataDir);
    }
  } finally {
    await browser.close();
  }
}
```

- [ ] **Step 3: provincial.ts 작성**

```typescript
// scripts/scrapers/provincial.ts
import { chromium } from 'playwright';
import type { ScraperConfig } from '../types';
import { parseRawText } from '../parser';
import { saveCandidateMd } from '../mdWriter';
import { extractCandidateRefs, extractAllPagesText } from './base';

export async function scrapeProvincial(
  config: ScraperConfig,
  dataDir: string
): Promise<void> {
  // ⚠️ SCRAPER_NOTES.md의 도의원 목록 URL로 교체
  const listingUrl = `https://policy.nec.go.kr/...?election=도의원&district=${encodeURIComponent(config.district)}`;

  const browser = await chromium.launch({ headless: true });
  try {
    const context = await browser.newContext();
    const page = await context.newPage();
    const refs = await extractCandidateRefs(page, listingUrl);
    console.log(`  도의원 후보 ${refs.length}명 발견`);
    for (const ref of refs) {
      console.log(`  수집 중: 기호${ref.ballotNumber} ${ref.name}`);
      const rawText = await extractAllPagesText(page, ref.documentKey);
      const candidate = parseRawText(rawText, config.electionType, config.region);
      saveCandidateMd(candidate, dataDir);
    }
  } finally {
    await browser.close();
  }
}
```

- [ ] **Step 4: municipal.ts 작성**

```typescript
// scripts/scrapers/municipal.ts
import { chromium } from 'playwright';
import type { ScraperConfig } from '../types';
import { parseRawText } from '../parser';
import { saveCandidateMd } from '../mdWriter';
import { extractCandidateRefs, extractAllPagesText } from './base';

export async function scrapeMunicipal(
  config: ScraperConfig,
  dataDir: string
): Promise<void> {
  // ⚠️ SCRAPER_NOTES.md의 시의원 목록 URL로 교체
  const listingUrl = `https://policy.nec.go.kr/...?election=시의원&district=${encodeURIComponent(config.district)}`;

  const browser = await chromium.launch({ headless: true });
  try {
    const context = await browser.newContext();
    const page = await context.newPage();
    const refs = await extractCandidateRefs(page, listingUrl);
    console.log(`  시의원 후보 ${refs.length}명 발견`);
    for (const ref of refs) {
      console.log(`  수집 중: 기호${ref.ballotNumber} ${ref.name}`);
      const rawText = await extractAllPagesText(page, ref.documentKey);
      const candidate = parseRawText(rawText, config.electionType, config.region);
      saveCandidateMd(candidate, dataDir);
    }
  } finally {
    await browser.close();
  }
}
```

- [ ] **Step 5: education.ts 작성**

```typescript
// scripts/scrapers/education.ts
import { chromium } from 'playwright';
import type { ScraperConfig } from '../types';
import { parseRawText } from '../parser';
import { saveCandidateMd } from '../mdWriter';
import { extractCandidateRefs, extractAllPagesText } from './base';

export async function scrapeEducation(
  config: ScraperConfig,
  dataDir: string
): Promise<void> {
  // ⚠️ SCRAPER_NOTES.md의 교육감 목록 URL로 교체
  const listingUrl = `https://policy.nec.go.kr/...?election=교육감&district=${encodeURIComponent(config.district)}`;

  const browser = await chromium.launch({ headless: true });
  try {
    const context = await browser.newContext();
    const page = await context.newPage();
    const refs = await extractCandidateRefs(page, listingUrl);
    console.log(`  교육감 후보 ${refs.length}명 발견`);
    for (const ref of refs) {
      console.log(`  수집 중: 기호${ref.ballotNumber} ${ref.name}`);
      const rawText = await extractAllPagesText(page, ref.documentKey);
      const candidate = parseRawText(rawText, config.electionType, config.region);
      saveCandidateMd(candidate, dataDir);
    }
  } finally {
    await browser.close();
  }
}
```

- [ ] **Step 6: 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 7: 커밋**

```bash
git add scripts/scrapers/
git commit -m "feat: election scrapers — governor, mayor, provincial, municipal, education"
```

---

## Task 9: collect.ts CLI 진입점

**Files:**
- Create: `scripts/collect.ts`

- [ ] **Step 1: collect.ts 작성**

```typescript
// scripts/collect.ts
import * as path from 'path';
import { updateMeta } from './cache';
import { scrapeGovernor } from './scrapers/governor';
import { scrapeMayor } from './scrapers/mayor';
import { scrapeProvincial } from './scrapers/provincial';
import { scrapeMunicipal } from './scrapers/municipal';
import { scrapeEducation } from './scrapers/education';
import type { ElectionType, ScraperConfig } from './types';

const DATA_DIR = path.join(process.cwd(), 'data');

/** 동탄5동 기준 선거 목록 */
const DONGTAN5_CONFIGS: ScraperConfig[] = [
  { electionType: '도지사',  region: '경기도',  district: '경기도' },
  { electionType: '시장',    region: '화성시',  district: '화성시' },
  { electionType: '도의원',  region: '경기도',  district: '동탄제1선거구' },
  { electionType: '시의원',  region: '화성시',  district: '동탄5동' },
  { electionType: '교육감',  region: '경기도',  district: '경기도' },
];

async function runScraper(config: ScraperConfig): Promise<void> {
  switch (config.electionType) {
    case '도지사':  return scrapeGovernor(config, DATA_DIR);
    case '시장':    return scrapeMayor(config, DATA_DIR);
    case '도의원':  return scrapeProvincial(config, DATA_DIR);
    case '시의원':  return scrapeMunicipal(config, DATA_DIR);
    case '교육감':  return scrapeEducation(config, DATA_DIR);
    default:
      throw new Error(`알 수 없는 선거 유형: ${config.electionType}`);
  }
}

async function main(): Promise<void> {
  const args = process.argv.slice(2);
  const typeIdx = args.indexOf('--type');
  const typeArg = typeIdx !== -1 ? (args[typeIdx + 1] as ElectionType) : undefined;

  const configs = typeArg
    ? DONGTAN5_CONFIGS.filter((c) => c.electionType === typeArg)
    : DONGTAN5_CONFIGS;

  if (configs.length === 0) {
    console.error(`오류: 알 수 없는 선거 유형 "${typeArg}"`);
    console.error(`가능한 값: ${DONGTAN5_CONFIGS.map((c) => c.electionType).join(', ')}`);
    process.exit(1);
  }

  console.log(`\n=== PolLens 데이터 수집 시작 ===`);
  console.log(`대상: ${configs.map((c) => `${c.electionType}(${c.district})`).join(', ')}\n`);

  const succeeded: ElectionType[] = [];
  const failed: ElectionType[] = [];

  for (const config of configs) {
    console.log(`\n[${config.electionType}] ${config.district} 수집 중...`);
    try {
      await runScraper(config);
      console.log(`[${config.electionType}] ✓ 완료`);
      succeeded.push(config.electionType);
    } catch (err) {
      console.error(`[${config.electionType}] ✗ 실패:`, err);
      failed.push(config.electionType);
    }
  }

  if (succeeded.length > 0) {
    updateMeta(DATA_DIR, succeeded);
  }

  console.log(`\n=== 수집 완료 ===`);
  console.log(`성공: ${succeeded.join(', ') || '없음'}`);
  if (failed.length > 0) {
    console.log(`실패: ${failed.join(', ')}`);
    process.exit(1);
  }
}

main().catch((err) => {
  console.error('예상치 못한 오류:', err);
  process.exit(1);
});
```

- [ ] **Step 2: 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 도움말 동작 확인**

```bash
npx ts-node scripts/collect.ts --type 없는선거
```

Expected:
```
오류: 알 수 없는 선거 유형 "없는선거"
가능한 값: 도지사, 시장, 도의원, 시의원, 교육감
```

- [ ] **Step 4: 커밋**

```bash
git add scripts/collect.ts
git commit -m "feat: collect.ts CLI — orchestrate all scrapers, cache update"
```

---

## Task 10: pollens.md 스킬

**Files:**
- Create: `skills/pollens.md`

- [ ] **Step 1: skills/pollens.md 작성**

```markdown
# /pollens — 2026 지방선거 공약 비교

경기도 화성시 동탄5동 기준으로 2026-06-03 지방선거 후보자들의 공약을 비교합니다.

---

## 실행 순서

### 1단계: 캐시 확인 및 갱신

아래 명령으로 meta.json을 확인한다:

\```bash
cat /Users/mksoo/Documents/dev/tmp/PolLens/data/meta.json 2>/dev/null || echo "NO_CACHE"
\```

- `collectedAt`이 24시간 이내 → 캐시 사용, 2단계로 바로 이동
- 파일 없음 또는 24시간 초과 → 사용자에게 안내 후 수집 실행:

> "데이터를 수집합니다. 약 30~60초 소요됩니다..."

\```bash
cd /Users/mksoo/Documents/dev/tmp/PolLens && npx ts-node scripts/collect.ts
\```

수집 실패 시: "데이터 수집에 실패했습니다. policy.nec.go.kr 접속을 확인하거나 잠시 후 다시 시도해주세요."

### 2단계: 선거 목록 안내

사용자에게 아래를 안내한다:

"경기도 화성시 동탄5동 기준으로 2026-06-03에 다음 선거가 예정되어 있습니다:

1. 도지사 — 경기도
2. 시장 — 화성시
3. 도의원 — 동탄제1선거구
4. 시의원 — 동탄5동
5. 교육감 — 경기도

어떤 선거부터 볼까요?"

### 3단계: 후보자 공약 비교

선택에 따라 해당 디렉토리의 모든 .md 파일을 읽는다:

| 선택 | 읽을 경로 |
|------|-----------|
| 도지사 | `/Users/mksoo/Documents/dev/tmp/PolLens/data/도지사/경기도/` |
| 시장 | `/Users/mksoo/Documents/dev/tmp/PolLens/data/시장/화성시/` |
| 도의원 | `/Users/mksoo/Documents/dev/tmp/PolLens/data/도의원/동탄제1선거구/` |
| 시의원 | `/Users/mksoo/Documents/dev/tmp/PolLens/data/시의원/동탄5동/` |
| 교육감 | `/Users/mksoo/Documents/dev/tmp/PolLens/data/교육감/경기도/` |

데이터 없음 시: "이 선거 데이터가 없습니다. `npx ts-node scripts/collect.ts`를 다시 실행해주세요."

파일을 읽은 후 **기호번호 오름차순**으로 정렬하여 비교표를 제시한다:

\```
## [선거명] 후보 공약 비교

| | 기호 1 · 이름 (정당) | 기호 2 · 이름 (정당) | ... |
|--|--|--|--|
| **공약 1** | [제목] | [제목] | ... |
| **공약 2** | [제목] | [제목] | ... |
| **공약 3** | [제목] | [제목] | ... |
| **공약 4** | [제목] | [제목] | ... |
| **공약 5** | [제목] | [제목] | ... |

※ 기호번호 순 정렬 / 판단·추천 없음
\```

### 4단계: 후속 안내

"다른 선거도 볼까요? 이 후보들에 대해 추가 정보를 찾아드릴까요?"

- **다른 선거 선택** → 2단계로 반복
- **추가 정보 요청** → "어떤 분야가 가장 중요하세요?" 질문 후 WebSearch로 후보자별 기사·발언 검색. 사실만 제시, 판단 없음.

---

## 절대 사용 금지 표현

"1위", "가장 적합", "추천", "승자", "점수", "더 나은 후보", "좋은 후보"

후보 정렬 기준: **기호번호(ballotNumber) 오름차순만** 허용.
```

- [ ] **Step 2: 커밋**

```bash
git add skills/pollens.md
git commit -m "feat: pollens.md Claude Code skill — conversation flow, comparison format"
```

---

## Task 11: 심볼릭 링크 등록 & 스모크 테스트

**Files:**
- Symlink: `~/.claude/commands/pollens.md` → `skills/pollens.md`

- [ ] **Step 1: 전체 테스트 최종 확인**

```bash
npx vitest run
```

Expected: parser, mdWriter, cache 모두 PASS

- [ ] **Step 2: 심볼릭 링크 등록**

```bash
ln -sf /Users/mksoo/Documents/dev/tmp/PolLens/skills/pollens.md \
       ~/.claude/commands/pollens.md
```

확인:
```bash
ls -la ~/.claude/commands/pollens.md
```

Expected: `pollens.md -> /Users/mksoo/Documents/dev/tmp/PolLens/skills/pollens.md`

- [ ] **Step 3: 단일 선거 스크래퍼 스모크 테스트**

Task 6 (사이트 탐색) 완료 후, 실제 셀렉터를 base.ts에 입력한 다음:

```bash
npx ts-node scripts/collect.ts --type 시장
```

Expected:
```
=== PolLens 데이터 수집 시작 ===
대상: 시장(화성시)

[시장] 화성시 수집 중...
  시장 후보 N명 발견
  수집 중: 기호1 홍길동
  → X페이지 추출 완료
  수집 중: 기호2 김철수
  → Y페이지 추출 완료
[시장] ✓ 완료

=== 수집 완료 ===
성공: 시장
```

생성된 파일 확인:
```bash
ls data/시장/화성시/
cat data/시장/화성시/*.md | head -20
```

Expected: 각 후보별 .md 파일 존재, 헤더와 공약 포함

- [ ] **Step 4: Claude Code에서 /pollens 실행 확인**

Claude Code 세션에서:
```
/pollens
```

Expected: 선거 목록 안내 메시지 출력

- [ ] **Step 5: 전체 수집 실행**

```bash
npx ts-node scripts/collect.ts
```

Expected: 5개 선거 모두 수집 성공, `data/meta.json` 생성

- [ ] **Step 6: 최종 커밋**

```bash
git add .
git commit -m "feat: PolLens complete — scraper + skill + symlink registered"
```
